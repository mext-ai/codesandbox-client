{
  "name": "codesandbox-client-static",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "serve www -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
